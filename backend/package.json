{
    "scripts": {
        "dfx_install": "DFX_VERSION=0.14.2 sh -ci \"$(curl -fsSL https://sdk.dfinity.org/install.sh)\"",
        "replica_start": "dfx start --background",
        "replica_stop": "dfx stop",
        "canister_deploy_local": "dfx deploy backend",
        "canister_deploy_mainnet": "dfx deploy --network ic backend",
        "canister_uninstall": "dfx canister uninstall-code backend"
    },
    "dependencies": {
        "azle": "0.17.1",
        "crypto": "^1.0.1"
    }
}
